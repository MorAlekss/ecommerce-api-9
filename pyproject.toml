[project]
name = "ecommerce-api"
version = "0.1.0"
description = "E-commerce API client — requests based (to be migrated to httpx async)"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
